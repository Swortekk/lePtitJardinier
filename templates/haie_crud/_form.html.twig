

<style>
    a{
        display: block;
        margin: 10px;
    }
    a .btn{
        margin-top:20px;
    }
</style>

{{ form_start(form) }}
<a>{{ form_row(form.code) }}</a>
<a>{{ form_row(form.nom) }}</a>
<a>{{ form_row(form.prix) }}</a>
<a>{{ form_row(form.categorie) }}</a>
<a> <button class="btn">{{ button_label|default('Enregistrer') }}</button></a>
{{ form_end(form) }}