{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block content %}
<style>
  .tableau  a {
        text-align: center;
        width: fit-content;
        margin-bottom: 20px;
    }

</style>
<div class="tableau">
    <h1>Categorie</h1>

    <table class="table table-striped table-bordered table-hover table-responsive ">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie.libelle }}</td>
            </tr>
        </tbody>
    </table>
    <div class="buttons"> <a class="btn"href="{{ path('app_categorie_c_r_u_d_index') }}">Retour</a>

        <a class="btn" href="{{ path('app_categorie_c_r_u_d_edit', {'id': categorie.id}) }}">Modifier</a>

        <a>{{ include('categorie_crud/_delete_form.html.twig') }}</a>
    </div>

</div>


{% endblock %}