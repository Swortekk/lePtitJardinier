{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block content %}
<div class="mid">
    <div class="tableau">
        <h1>Categorie</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ categorie.id }}</td>
                </tr>
                <tr>
                    <th>Libelle</th>
                    <td>{{ categorie.libelle }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_categorie_c_r_u_d_index') }}">back to list</a>

        <a href="{{ path('app_categorie_c_r_u_d_edit', {'id': categorie.id}) }}">edit</a>

        {{ include('categorie_crud/_delete_form.html.twig') }}
    </div>
</div>

{% endblock %}