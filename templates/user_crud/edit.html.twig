{% extends 'base.html.twig' %}

{% block title %}Edit User
{% endblock %}

{% block content %}
<div class="mid">
	<div class="formulaire">
		<h1>Edit User</h1>

		{{ include('user_crud/_form.html.twig', {'button_label': 'Update'}) }}

		{% if is_granted('ROLE_ADMIN') %}
		<a href="{{ path('app_user_c_r_u_d_index') }}">liste des utilisateurs</a>
		{% else %}
		<a href="{{ path('app_user_c_r_u_d_show', {'id': user.id}) }}">retour</a>
		{% endif %}

		{{ include('user_crud/_delete_form.html.twig') }}
	</div>
</div>
{% endblock %}