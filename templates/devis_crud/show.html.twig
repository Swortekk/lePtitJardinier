{% extends 'base.html.twig' %}

{% block title %}Devis{% endblock %}

{% block body %}
{% block content %}
<h1>Devis</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ devi.id }}</td>
        </tr>
        <tr>
            <th>Date</th>
            <td>{{ devi.date ? devi.date|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>Longueur</th>
            <td>{{ devi.longueur }}</td>
        </tr>
        <tr>
            <th>Hauteur</th>
            <td>{{ devi.hauteur }}</td>
        </tr>
    </tbody>
</table>

<a href="{{ path('app_devis_c_r_u_d_index') }}">back to list</a>

<a href="{{ path('app_devis_c_r_u_d_edit', {'id': devi.id}) }}">edit</a>

{{ include('devis_crud/_delete_form.html.twig') }}
{% endblock %}
{% endblock %}